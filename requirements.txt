# ============================================================================
# AQuant 依赖清单 —— 按层级分组，安装：pip install -r requirements.txt
# Python 版本要求：3.9+（当前开发环境 3.13）
# 版本策略：>= 下界，允许小版本升级；如遇兼容性问题可固定主版本。
# ============================================================================

# ---- Layer 1 数据层：实时行情与财务数据 ----
# >=1.18.0：包含 stock_zh_valuation_baidu（百度股市通估值接口）与新浪备用源支持
akshare>=1.18.0

# ---- Layer 2 因子计算层：向量化指标计算 ----
pandas>=2.2.0
numpy>=1.26.0

# ---- Layer 0 配置层：settings.yaml 加载 ----
PyYAML>=6.0.1

# ---- 环境变量：.env 读取（dotenv） ----
python-dotenv>=1.0.1

# ---- Layer 4/5 网络层：百炼 API、PushPlus 微信推送 ----
requests>=2.31.0

# ---- Layer 4 AI 层：阿里云百炼（OpenAI 兼容接口 + Pydantic-AI 结构化输出） ----
openai>=1.40.0
# pydantic-ai v2：Agent + output_type 自动 JSON 提取 / Schema 校验 / 重试
# 安装方式：pip install pydantic-ai
pydantic-ai>=2.0.0

# ---- 测试（TDD）----
pytest>=8.0.0
# pytest-markdown-docs：无需额外安装，标记注册在 pytest.ini 中完成
# 冒烟测试运行方式：python -m pytest -m smoke -v

# ---- Phase 5 本地回测（可选，安装较重，需要时再装） ----
# vectorbt>=0.28.0
